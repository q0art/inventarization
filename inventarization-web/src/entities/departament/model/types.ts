export type Departament = {
  id: string;
  name: string;
  createdAt: string;
  updatedAt: string;
};

export type CreateDepartamentDto = {
  name: string;
};

export type UpdateDepartamentDto = {
  name: string;
};
